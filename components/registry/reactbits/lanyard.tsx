"use client";
// react-bits Lanyard requires user-provided 3D assets (`card.glb`,
// `lanyard.png`) that are NOT shipped with the registry. The original
// component is preserved as the upstream reference at
// https://reactbits.dev/components/Lanyard - copy it back into this
// file and place the assets alongside it when you need it. For now
// this is a no-op render so the registry does not break the test page.
export function Lanyard(_props: { className?: string }) {
  return null;
}
export default Lanyard;
